import 'package:flutter/material.dart';

class AdsShowTimeMobile extends StatelessWidget {
  const AdsShowTimeMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
