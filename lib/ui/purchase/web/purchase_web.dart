import 'package:flutter/material.dart';

class PurchaseWeb extends StatelessWidget {
  const PurchaseWeb({super.key});

  ///Build Override
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
