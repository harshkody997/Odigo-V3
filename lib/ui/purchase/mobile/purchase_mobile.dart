import 'package:flutter/material.dart';

class PurchaseMobile extends StatelessWidget {
  const PurchaseMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
