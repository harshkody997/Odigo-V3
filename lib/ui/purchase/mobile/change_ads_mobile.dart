import 'package:flutter/material.dart';

class ChangeAdsMobile extends StatelessWidget {
  const ChangeAdsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
