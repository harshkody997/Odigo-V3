import 'package:flutter/material.dart';

class PurchaseDetailsMobile extends StatelessWidget {
  const PurchaseDetailsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
