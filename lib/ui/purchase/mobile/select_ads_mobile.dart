import 'package:flutter/material.dart';

class SelectAdsMobile extends StatelessWidget {
  const SelectAdsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
