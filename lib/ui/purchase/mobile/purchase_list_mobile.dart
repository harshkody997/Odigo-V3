import 'package:flutter/material.dart';

class PurchaseListMobile extends StatelessWidget {
  const PurchaseListMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
